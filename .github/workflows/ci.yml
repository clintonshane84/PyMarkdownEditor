name: CI

on:
  push:
    branches: [ master, development ]
  pull_request:
    branches: [ master, development ]

permissions:
  contents: read

jobs:
  test:
    name: Lint & Test (${{ matrix.os }}, py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    env:
      QT_QPA_PLATFORM: offscreen  # headless Qt
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PYTHONDONTWRITEBYTECODE: "1"
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.10"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install -U pip
          if exist requirements.txt (pip install -r requirements.txt) || true
          if exist dev-requirements.txt (pip install -r dev-requirements.txt) || true
        shell: bash

      - name: Ruff format (check)
        run: |
          ruff format --check .
          ruff check .
        shell: bash

      - name: Run tests
        run: |
          pytest --cov=pymd --cov-report=term-missing -q
        shell: bash
